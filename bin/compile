#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# fail fast
set -e

# config
APR_VERSION="1.5.2"
APACHE_VERSION="2.4.10"
APACHE_PATH="apache"
PHP_VERSION="7.0.5"
PHP_PATH="php"

BIN_DIR=$(dirname $0)
BUILD_DIR=$1
CACHE_DIR=$2
LP_DIR=`cd $(dirname $0); cd ..; pwd`

# include .files when moving things around
shopt -s dotglob

cd $BUILD_DIR

APR_URL="http://mirror.cogentco.com/pub/apache/apr/apr-$APR_VERSION.tar.gz"
echo "-----> Downloading Apache Portable Runtime version $APR_VERSION"
curl --location --silent --max-time 60 --location "$APR_URL" | tar xz

echo "-----> Building Apache Portable Runtime version $APR_VERSION"
cd apr-1.5.1
./configure --prefix=/app/apr
make
make test
make install

# TODO: Download, Build and Install APR-util

APACHE_URL="http://mirror.cogentco.com/pub/apache/httpd/httpd-$APACHE_VERSION.tar.gz"
echo "-----> Downloading Apache version $APACHE_VERSION"
curl --location --silent --max-time 60 --location "$APACHE_URL" | tar xz

# TODO: Build and Install Apache

PHP_URL="https://php.net/get/php-$PHP_VERSION.tar.gz/from/this/mirror"
echo "-----> Downloading PHP version $PHP_VERSION"
curl --location --silent --max-time 60 --location "$PHP_URL" | tar xz

# TODO: Build and Install PHP

# update config files
cp $LP_DIR/conf/httpd.conf $APACHE_PATH/conf
cp $LP_DIR/conf/php.ini php

# make php available on bin
mkdir -p bin
ln -s /app/php/bin/php bin/php

cat >>boot.sh <<EOF
for var in \`env|cut -f1 -d=\`; do
  echo "PassEnv \$var" >> /app/apache/conf/httpd.conf;
done
touch /app/apache/logs/error_log
touch /app/apache/logs/access_log
tail -F /app/apache/logs/error_log &
tail -F /app/apache/logs/access_log &
export LD_LIBRARY_PATH=/app/php/ext
export PHP_INI_SCAN_DIR=/app
echo "Launching apache"
exec /app/apache/bin/httpd -DNO_DETACH
EOF

chmod +x boot.sh

# clean the cache
rm -rf $CACHE_DIR/*
